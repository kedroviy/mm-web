import { HideEmptyErrorDirective } from './hide-empty-error';

describe('HideEmptyError', () => {
  it('should create an instance', () => {
    const directive = new HideEmptyErrorDirective();
    expect(directive).toBeTruthy();
  });
});
